# objekt_api

## A Python Wrapper to Access the APIs in objekt.top

This package attempts to make it easier to pull information pertaining to objekts tracked in objekts.top.

## Getting Started

`objekt_api` requires Python 3.9 and leverages `requests` to communicate with objekts.top.

```
pip install objekt_api
```

